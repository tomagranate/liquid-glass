# Implementation Plans

Generated by the improve skill on 2026-06-15, against commit `ae5ad18`. Each
plan is self-contained: read it fully before starting, honor its STOP
conditions, and update your status row when done.

Scope of this round: the maintainer's directives from the session — (1) stop
shipping pre-styled React components (keep them as examples), and (2) refocus
the README on install + usage with media, moving the deep technical content into
its own doc. Package boundary was decided by the maintainer: **keep the React
kit (`GlassStage` + `useGlassLens`/`useGlassStage`); move only the pre-styled
widgets.**

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Ship engine + React kit only; move widgets to examples | P1 | M | — | DONE & MERGED (PR #1, squashed onto master as `489d127`) |
| 002 | Refocus README; add ARCHITECTURE.md; wire in media | P1 | M | 001 | DONE (executor; commit `86748c0`, branch `worktree-agent-af57273663fb67570` — not yet merged; **manual media capture still pending, see below**) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

## Dependency notes

- **002 requires 001.** The new README quick-start documents the post-001
  surface (no pre-styled widgets imported from the package; widgets pointed to
  `examples/demo/src/glass`). Writing the README before 001 would re-document
  the old API.

## Manual follow-ups (cannot be done by an executor model)

- **Demo media** (from Plan 002, Step 4): capture `assets/hero.gif` and
  `assets/components.png` from the running demo (`npm run dev`) per
  `assets/README.md`, then commit them. Animated GIFs require screen recording.
  Until captured, the README has two broken image links.

## Findings considered and rejected

- *Drop React entirely / ship engine-only (React-free package)*: considered as
  the alternative package boundary. Rejected by the maintainer in favor of
  keeping the React kit (`GlassStage` + hooks) shipped, with only the pre-styled
  widgets moved to examples. (Recorded so it isn't re-litigated.)
- *Change the default backend / make `applyGlass` the React default too*: out of
  scope. No code-level default change this round — the repositioning toward
  `applyGlass` for typical sites is handled as framing in the README/ARCHITECTURE
  docs (Plan 002), not by altering `GlassStage`.
